# Automation Pipeline (Scaffold)

This is a primitive async automation pipeline scaffold.  
Main features:
- Async task execution using `asyncio`
- Logging results to `.pkl` files
- Simple structure to build on

## Run
```bash
python -m pipeline.main
